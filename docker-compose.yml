services:
  nansen:
    build:
      dockerfile: ./Dockerfile
      context: .
    image: nansen
    volumes:
      - ./nansen:/app/nansen
      - ./.git:/app/.git
      - ./data:/data
    command: python -m build --wheel
